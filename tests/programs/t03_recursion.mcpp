int fib(int n) {
    if (n < 2) { return n; }
    return fib(n - 1) + fib(n - 2);
}
int main() {
    int i = 0;
    for (i = 0; i < 10; i = i + 1) {
        print_int(fib(i));
        print_char(32);
    }
    print_char(10);
    return 0;
}
